# Build and generated output
build
build-firefox
dist
docs
coverage
demo/output

# Gitignored scratch
dev

# Git submodule — belongs to jhyland87/typedoc-theme-hierarchy, not this repo
themes

# Locale files are hand-formatted and MUST NOT be reserialized — placeholder
# blocks are laid out differently per locale, and a reformat rewrites every key.
# See the add-i18n-key skill.
src/_locales

# Generated by tools/generate-supplier-constants.js; the generator's template is
# the single source of formatting truth for this file.
src/constants/suppliers.ts

# Lockfile
pnpm-lock.yaml
